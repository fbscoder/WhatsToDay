<!DOCTYPE html>
<head>
    <title></title>
    <meta charset="utf-8">
    <meta content="width=device-width, initial-scale=1" name="viewport">
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" rel="stylesheet">
    <link crossorigin="anonymous" href="https://use.fontawesome.com/releases/v5.5.0/css/all.css"
          integrity="sha384-B4dIYHKNBt8Bc12p+WXckhzcICo0wtJAoU8YZTY5qE0Id1GSseTk6S+L3BlXeVIU" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">
    <link href="css/style.css" rel="stylesheet" type="text/css">
    <!---->
    <style>

    </style>

</head>
<div class="container-fluid">
    <div class="timeButtons">
        <span><button id="btn_today">Heute ( {{ taskTodayCount }} )</button></span><br>
        <span><button id="btn_tomorrow">Morgen ( {{ taskTomorrowCount }} )</button></span>
    </div>
    <div class="row" id="tasks">
        <div class="col-md-12">
            <a href="#" class="btn btn-sm btn-default" id="hideTasks"><i class="fas fa-arrow-circle-left"></i></a>
            <div class="tasks" id="task_today">
                {% for task in taskToday %}
                    <div class="card text-center task_card">
                        <div class="card-header collapsed" style="cursor:pointer;" data-toggle="collapse"
                             href="#task_{{ loop.index }}"
                             aria-controls="task_{{ loop.index }}">
                            {{ task.title }}
                        </div>
                        <div class="collapse" id="task_{{ loop.index }}">
                            <div class="card-body ">
                                <p class="card-text">{{ task.description }}</p>
                                <ul>
                                    {% if task.checkListData != null %}
                                        {% for checklist in task.checkListData %}
                                            {{ checklist.name }}
                                            {% if checklist.check %}
                                                {% for subTask in checklist.check %}
                                                    <li>
                                                        <div class="custom-control custom-checkbox">
                                                            <input type="checkbox" class="custom-control-input"
                                                                   id="checkBox_{{ loop.index }}" disabled
                                                                   {% if subTask.isChecked %}checked{% endif %}>
                                                            <label class="custom-control-label"
                                                                   for="checkBox_{{ loop.index }}">{{ subTask.name }}</label>
                                                        </div>
                                                    </li>
                                                {% endfor %}
                                            {% endif %}
                                        {% endfor %}
                                    {% endif %}
                                </ul>
                            </div>
                            <div class="card-footer text-muted">
                                <a href="#" class="btn btn-gray">FINISH</a>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
            <div class="tasks" id="task_tomorrow">
                {% for task in taskTomorrow %}
                    <div class="card text-center task_card">
                        <div class="card-header collapsed" style="cursor:pointer;" data-toggle="collapse"
                             href="#task_{{ loop.index }}"
                             aria-controls="task_{{ loop.index }}">
                            {{ task.title }}
                        </div>
                        <div class="collapse" id="task_{{ loop.index }}">
                            <div class="card-body ">
                                <p class="card-text">{{ task.description }}</p>
                                <ul>
                                    {% if task.checkListData != null %}
                                        {% for checklist in task.checkListData %}
                                            <p>{{ checklist.name }}</p>
                                            {% if checklist.check %}
                                                {% for subTask in checklist.check %}
                                                    <li>
                                                        <div class="custom-control custom-checkbox">
                                                            <input type="checkbox" class="custom-control-input"
                                                                   id="checkBox_{{ loop.index }}" disabled
                                                                   {% if subTask.isChecked %}checked{% endif %}>
                                                            <label class="custom-control-label"
                                                                   for="checkBox_{{ loop.index }}">{{ subTask.name }}</label>
                                                        </div>
                                                    </li>
                                                {% endfor %}
                                            {% endif %}
                                        {% endfor %}
                                    {% endif %}
                                </ul>
                            </div>
                            <div class="card-footer text-muted">
                                <a href="#" class="btn btn-gray">FINISH</a>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>

</div>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js"></script>
<script src="js/script.js"></script>
