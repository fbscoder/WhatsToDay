{% extends 'base.html.twig' %}
{% block body %}
    <div class="container-fluid">
        <div class="row" id="row_heute">
            <div class="col" id="div_heute">
                <ul>
                    {% for board in boards %}
                        <li style="text-wrap: normal">{{ board.name }}</li>
                    {% endfor %}
                </ul>
            </div>
        </div>
    </div>

{% endblock %}